<Window x:Class="WpfAppGlobalShoes.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfAppGlobalShoes"
        Title="Loading" 
        WindowStyle="None" 
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Height="1080" Width="1920">

    <Grid>
        <!-- Background -->
        <Rectangle Fill="White" RadiusX="10" RadiusY="10" />

        <!-- StackPanel to center elements vertically -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <!-- Loading Logo -->
            <Image Source="pack://application:,,,/Images/GSLogo.png" Height="200" Margin="0,0,0,20" />

            <!-- Loading GIF -->
            <local:GifImage x:Name="LoadingGif" Width="100" Height="100" Margin="0,0,0,20" />

            
        </StackPanel>
    </Grid>
</Window>
