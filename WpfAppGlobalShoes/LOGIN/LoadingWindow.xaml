<Window x:Class="WpfAppGlobalShoes.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading" 
        WindowStyle="None" 
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Height="1080" Width="1920">

    <Grid>
        <!-- Background -->
        <Rectangle Fill="White" RadiusX="10" RadiusY="10" />

        <!-- Loading Logo -->
        <Image x:Name="LogoImage" Source="pack://application:,,,/Images/GSLogo.png" Width="200" Height="200" 
               HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image.RenderTransform>
                <RotateTransform x:Name="LogoRotation" />
            </Image.RenderTransform>
        </Image>

        <!-- Loading Text -->
        
    </Grid>
</Window>
